// este código não será executavel

// Antes: Linguagens não estruturadas (go to) | Depois: Linguagens estruturadas (procedural e OO se encaixam)
// Procedural(antes da OO(Orientação a Objetos)) 
processamento(valor1, valor2, valor3)

 // OO
 objeto = {
     valor1,
     valor2,
     valor3,
     processamento() {
         // ...
     }
 }

 objeto.processamento() // Foco passou a ser o objeto

 // Principios importantes(Serão vistos):
 // 1. abstracao
 // 2. encapsulamento
 // 3. herança (prototype)
 // 4. polimorfismo