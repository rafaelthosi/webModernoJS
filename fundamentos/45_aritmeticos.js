const [a, b, c, d] = [3, 6, 9, 12]

const soma = a + b
const subtracao = b - c
const multiplicacao = c * d
const divisao = d / a
const modulo = a % 2
//operadores aritméticos são operadores binarios, pois trabalham com dois operando. Um operador unario seria -2(mais exemplos: -soma, -subtracao. Estes deixam as variaveis negativas)

console.log(soma, subtracao, multiplicacao, divisao, modulo)